// GENERADO: este archivo se regenera con `bash scripts/generate-types.sh`
// después de cada migración (regla 6 / doc 05 §5). Placeholder hasta la
// primera migración aplicada en el proyecto Supabase.
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
  };
};
