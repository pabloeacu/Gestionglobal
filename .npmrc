# @zoom/meetingsdk declara peer react@18.2.0 (exacto) y nosotros estamos en
# 18.3.1. La diferencia minor es benigna; usamos legacy resolver para que
# Vercel pueda instalar sin --legacy-peer-deps en la línea de comandos.
legacy-peer-deps=true
